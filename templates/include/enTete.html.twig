

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    {% if app.user %}
        <div class="logo">
            <a href="{{ path('main_accueil') }}"><img src="{{ asset('imageLogo/logoIcon.png') }}" alt="logo"></a>
        </div>
        <div class="nav-item ml-5">
            <a class=" navbar-brand " href="{{ path('main_accueil') }}">Bucket-list</a>
        </div>
    {% endif %}
    <div class="logo">
        <img src="{{ asset('imageLogo/logoIcon.png') }}" alt="logo">
    </div>
    <div class="nav-item ml-5">
        <div class="navbar-brand">Sortir.com</div>
    </div>
    {% if app.user %}
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse " id="navbarNav" style="justify-content: flex-end">
            <div class="row">
                <div class="col-lg-4">
                    <div class="container">
                        <ul class="navbar-nav no-wrap me-auto mb-2 mb-lg-0">
                            <li class="nav-item">
                                <a class=" nav-link active  " href="{{ path('main_accueil') }}" title="Accueil">Accueil</a>
                            </li>
                            <li class="nav-item " style="white-space: nowrap;">
                                <a class=" nav-link  " href="{{ path('main_aboutUs') }}" title="Nous">About Us</a>
                            </li>
                            <li class="nav-item">
                                <a class=" nav-link " href="{{ path('wish_listes') }}" title="Wish-List">WishList</a>
                            </li>
                            {% if app.user %}
                                <li class="nav-item">
                                    <a class=" nav-link " href="{{ path('main_formulaire') }}" title="Ajouter">+Wish</a>
                                </li>
                                <li class="nav-item">
                                    <div class="dropdown">
                                        <button class="btn btn-secondary dropdown-toggle" type="button" id="deroulantb" data-toggle="dropdown" >
                                            {{ app.user.pseudo }}
                                        </button>
                                        <div class="dropdown-menu dropdown-menu-lg-right " aria-labelledby="deroulantb" >
                                            <p class="dropdown-item"  >Votre adresse :<br> {{ app.user.email }}</p><hr>
                                            <a class="dropdown-item" href="{{ path('user_modification',{'id': app.user.id}) }}" title="modifier">Profil</a><hr>
                                            <a class="  dropdown-item " href="{{ path('app_logout') }}" title="logout">
                                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-vinyl-fill" viewBox="0 0 16 16" style="color: red;">
                                                    <path d="M8 6a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm0 3a1 1 0 1 1 0-2 1 1 0 0 1 0 2z"/>
                                                    <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zM4 8a4 4 0 1 0 8 0 4 4 0 0 0-8 0z"/>
                                                </svg>
                                                Deconnexion</a>
                                        </div>
                                    </div>
                                </li>
                            {% else %}
                                <li class="nav-item">
                                    <a style="white-space: nowrap;" class=" nav-link " href="{{ path('app_login') }}" title="Ajouter">Connectez-vous</a>
                                </li>
                                <li class="nav-item">
                                    <a class=" nav-link " href="{{ path('app_register') }}" title="Ajouter">Inscription</a>
                                </li>
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    {% endif %}
</nav>
